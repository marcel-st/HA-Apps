name: "SSH Call Home"
description: "Reverse SSH tunnel to external server"
version: "1.3.0"
slug: "ssh_call_home"
arch:
  - aarch64
  - amd64
  - armv7
init: false
ingress: true
ingress_port: 8099
panel_title: "SSH Call Home"
panel_icon: "mdi:cloud-refresh"
hassio_api: true
options:
  ssh_host: ""
  ssh_port: 
  ssh_user: ""
  remote_port: 
  private_key: ""
  update_ui: true
schema:
  ssh_host: str
  ssh_port: int
  ssh_user: str
  remote_port: int
  private_key: password
  update_ui: bool
